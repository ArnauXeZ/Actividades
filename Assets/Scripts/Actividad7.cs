using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Actividad7 : MonoBehaviour
{
    public string letra = "a";
    public void Start()
    {
        if (letra != "a" || letra != "e" || letra != "i" || letra != "o" || letra != "u" )
        Debug.Log("Es vocal");
        
        else if
        Debug.Log("Es consonante");
       



    }    
}
